import { IfFormErrorDirective } from './if-form-error.directive';

describe('IfFormErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new IfFormErrorDirective();
    expect(directive).toBeTruthy();
  });
});
